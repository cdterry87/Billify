<div class="flex flex-col gap-1 items-center justify-center text-center select-none">
    <h1 class="logo text-6xl font-bold text-primary"><a href="{{ route('home') }}">{{ config('app.name') }}</a></h1>
    <h2 class="logo text-3xl font-semibold text-secondary">{{ config('app.tagline') }}</h2>
</div>
